services:
  - type: web
    name: plinth-backend
    runtime: docker
    plan: free
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        value: https://project-haven-pi.vercel.app
